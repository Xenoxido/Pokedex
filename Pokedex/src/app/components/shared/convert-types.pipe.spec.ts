import { ConvertTypesPipe } from './convert-types.pipe';

describe('ConvertTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
